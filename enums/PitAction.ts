export enum PitAction {
  CHECKOUT = 'checkout',
  CHECKOUT_ALIAS = 'co',
  CHECKOUT_DEVELOP = 'cod',
  CHECKOUT_MASTER = 'com',
  REVIEW = 'review',
  REVIEW_ALIAS = 'rv',
  CLEAN = 'clean',
  CLEAN_ALIAS = 'cl',
  PULL_ORIGIN = 'pulo',
  DOCKER = 'docker',
  DOCKER_ALIAS = 'd',
  JIRA = 'jira',
  USER = 'user',
  HELP = 'help',
  DEBUG = 'debug',
}
